package org.computerShop.repository;

import org.computerShop.model.ProductSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductSectionRepo extends JpaRepository<ProductSection, Long> {
}
